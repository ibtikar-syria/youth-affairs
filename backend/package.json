{
  "name": "backend",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "typecheck": "tsc --noEmit",
    "db:migrate": "wrangler d1 migrations apply youth-affairs-db --local"
  },
  "dependencies": {
    "hono": "^4.12.1"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "wrangler": "^4.4.0"
  }
}